//задовільняє

//some every

const arr1 = [21, 4, 83];

const isEven = function (number) {
  return number % 2 === 0;
};

// console.log(arr1.some(isEven))  //||
// console.log(arr1.every(isEven)) //&&
